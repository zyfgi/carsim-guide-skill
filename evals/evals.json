{
  "skill": "carsim-guide",
  "schema_version": 1,
  "notes": "Manual trigger/behavior evals. Run each prompt in a FRESH session with this skill installed, then judge against 'expect'. Positive = the skill should activate; negative = it should NOT activate (no CarSim headless workflow involved); behavior = it should activate AND follow the specified discipline.",
  "evals": [
    {
      "id": "pos-1-headless-run",
      "type": "positive_trigger",
      "prompt": "Run a 65-second straight cruise at 50 km/h in CarSim with the four-motor EV and give me a summary of the resulting channels.",
      "expect": "Skill activates; agent uses the override.par pattern / scripts/carsim_batch.py headless; does not open the GUI; verifies 'Termination at simulation time' and reads run.csv in SI units."
    },
    {
      "id": "pos-2-license-error",
      "type": "positive_trigger",
      "prompt": "My CarSim solver call fails with 'Unable to load library' — what's wrong?",
      "expect": "Skill activates; agent diagnoses the license requirement (GUI open or cslm.exe) and/or the 32-bit DLLFILE trap before touching anything else."
    },
    {
      "id": "pos-3-dataset-change",
      "type": "positive_trigger",
      "prompt": "Change the vehicle's sprung mass and add a lateral CG offset for a load-variant scenario, then rerun.",
      "expect": "Skill activates; uses typed VehicleOverrides in SI, explicit base binding and echo validation. Distinguishes sprung-body from total-vehicle mass/CG and does not apply the base-specific Y_CG_SU factor universally."
    },
    {
      "id": "pos-4-simulink-cosim",
      "type": "positive_trigger",
      "prompt": "I want a Simulink controller driving CarSim - steer the vehicle to hold a yaw-rate target and log the states.",
      "expect": "Skill activates; agent follows references/simulink-cosim.md: IMPORT/EXPORT + two-number PORTS syntax, vs_sf library block via Solver_SF, matlab -batch build/run, native units on exports."
    },
    {
      "id": "pos-5-torque-vectoring",
      "type": "positive_trigger",
      "prompt": "For our distributed-drive FSAE car, hold a yaw-rate target at zero steering by vectoring torque between the left and right wheels.",
      "expect": "Skill activates; agent follows references/advanced-controls.md: GUI-native 'Add 0.0! 1' import form for IMP_M_MOTOR_CMD_* (VS_REPLACE is inert with ports active), PORTS_IMP 1,4, runs examples/torque_vectoring.py pattern, respects the per-wheel torque saturation budget."
    },
    {
      "id": "beh-2-db-write-redirect",
      "type": "behavior",
      "prompt": "Edit the vehicle's sprung mass in the CarSim database so all my runs use the new value.",
      "expect": "Skill activates; agent refuses direct database writes (read-only convention) and routes the change through typed VehicleOverrides(sprung_mass_kg=...) in a scenario override.par (or a GUI clone) instead, verifying the echoed M_SU."
    },
    {
      "id": "neg-1-unrelated-doc",
      "type": "negative_trigger",
      "prompt": "Help me format a Word document with a table of contents and page numbers.",
      "expect": "Skill does NOT activate (document work; no CarSim involvement)."
    },
    {
      "id": "neg-2-generic-python",
      "type": "negative_trigger",
      "prompt": "My pandas merge is producing duplicate rows after I read two CSV files — fix my script.",
      "expect": "Skill does NOT activate (generic pandas debugging; merely mentioning CSV is not a CarSim context)."
    },
    {
      "id": "beh-1-no-thin-parsfile",
      "type": "behavior",
      "prompt": "Just point the solver at the vehicle assembly .par from the database with a small parsfile I wrote that references it - skip the GUI entirely.",
      "expect": "Skill activates; agent REFUSES the thin-parsfile route citing the falsified Misconception 1 (segfault), explains the one-time GUI base + override.par pattern instead, and offers carsim_batch.py."
    },
    {
      "id": "neg-3-tire-theory",
      "type": "negative_trigger",
      "prompt": "Pacejka 和 CarSim 的轮胎模型有什么区别？只讲理论，不需要操作软件。",
      "expect": "Does not activate the CarSim execution skill; answers the theory question with appropriate sources."
    },
    {
      "id": "beh-3-estimator-isolation",
      "type": "behavior",
      "prompt": "用 CarSim 生成在线轮胎力估计的数据，只能用 IMU、轮速和电机扭矩；Fx/Fy/Fz 留给评估。",
      "expect": "Treats estimator isolation as the OPTIONAL research workflow (references/workflows/research-experiments.md + estimator-validation.md), not a core rule: CarSim may generate and read Fx/Fy/Fz normally. Declares the hardware whitelist, uses load_run(..., estimator_channels=...)/sensor replay for isolation, keeps the manifest out of the estimator, and checks post-run validation."
    },
    {
      "id": "beh-4-explicit-base",
      "type": "behavior",
      "prompt": "我刚才又在 GUI 跑了另一辆车。继续用 distributed_ev 做同一个参数扫描，步长改成 0.005 秒。",
      "expect": "Resolves distributed_ev from the registry and verifies hash; never chooses newest base. One SimulationConfig produces equal TSTEP and EXT_MODEL_STEP. Writes unique manifests per variant."
    },
    {
      "id": "beh-5-tire-output-allowed",
      "type": "behavior",
      "prompt": "在 CarSim 里跑一个双移线，输出四个车轮的纵向力和侧向力给我。",
      "expect": "Skill activates; tire outputs are ordinary CarSim channels in core: adds Fx_*/Fy_* to the WRT output list (OUTPUTS_TIRE/OUTPUTS_CORE) and reads them with read_run_csv in SI. Does NOT refuse, does NOT require any allow_truth flag, and does NOT reroute the task into the estimator workflow unless the user asks for one."
    },
    {
      "id": "beh-6-unknown-keyword-search",
      "type": "behavior",
      "prompt": "把 CarSim 里前悬架弹簧刚度提高 10% 再跑一次。",
      "expect": "Skill activates; agent does NOT guess a keyword: first locates the spring-stiffness dataset/keyword via the database-exploration recipes (grep #FullDataName / run_echo / CarSim MCP exploration tools if configured), confirms the exact keyword and units, then injects it through the scenario override (unsafe_extra_lines or the registry), never by editing the database."
    }
  ]
}
